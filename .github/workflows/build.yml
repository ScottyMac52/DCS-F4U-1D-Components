name: Build OvGME package

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# contents: write required so commit-kneeboard can push regenerated SVG/PNG to main.
permissions:
  contents: write

jobs:
  build:
    uses: ScottyMac52/DCS-Common/.github/workflows/build.yml@main
    with:
      package-version: 0.0.0-ci.${{ github.run_number }}
      install-lua-parser: true
      run-lua-parse: true
      extra-validation-command: npm run test:profile
      package-script: scripts/Build-OvGME.ps1
      test-package-script: scripts/Test-Package.ps1
      build-release-script: scripts/Build-Release.ps1
      test-release-script: scripts/Test-Package.ps1
      # Keep repo kneeboard assets in lockstep with the OvGME package.
      # Skipped on pull_request; runs on main push / workflow_dispatch.
      commit-kneeboard: true
      kneeboard-paths: kneeboard/source kneeboard/F4U-1D
      artifact-name: F4U-1D-OvGME-package
      artifact-path: |
        dist/*.zip
        dist/SHA256SUMS.txt
