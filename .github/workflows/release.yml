name: Create tagged OvGME release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: Semantic version component to increment
        required: true
        default: minor
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

concurrency:
  group: ovgme-release
  cancel-in-progress: false

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      BUMP: ${{ inputs.bump }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install Lua parser
        run: sudo apt-get update && sudo apt-get install -y lua5.4
      - name: Install build dependencies
        run: npm ci
      - name: Calculate next version
        id: version
        shell: pwsh
        run: |
          $LatestTag = git tag --list 'v*' --sort=-version:refname |
            Where-Object { $_ -match '^v(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)$' } |
            Select-Object -First 1

          if ([string]::IsNullOrWhiteSpace($LatestTag)) {
            $LatestVersion = '0.0.0'
          } else {
            $LatestVersion = $LatestTag.Substring(1)
          }

          $Version = node ./scripts/version.mjs next $LatestVersion $env:BUMP
          if ($LASTEXITCODE -ne 0) { throw 'Failed to calculate the next semantic version.' }
          $Tag = "v$Version"
          if (git tag --list -- $Tag) { throw "Tag $Tag already exists." }

          $ReleaseSha = git rev-parse HEAD
          if ($LatestTag -and (git rev-list -n 1 $LatestTag) -eq $ReleaseSha) {
            throw "There are no commits on main after $LatestTag."
          }

          "PACKAGE_VERSION=$Version" >> $env:GITHUB_ENV
          "RELEASE_TAG=$Tag" >> $env:GITHUB_ENV
          "RELEASE_SHA=$ReleaseSha" >> $env:GITHUB_ENV
          "tag=$Tag" >> $env:GITHUB_OUTPUT
      - name: Test semantic versioning
        run: npm run test:versioning
      - name: Validate control profiles
        run: npm run test:profile
      - name: Parse Lua profiles
        shell: pwsh
        run: |
          Get-ChildItem src -Recurse -Filter '*.lua' | ForEach-Object {
            & luac5.4 -p $_.FullName
            if ($LASTEXITCODE -ne 0) { throw "Invalid Lua: $($_.FullName)" }
          }
      - name: Generate and validate OpenKneeboard pages
        run: |
          npm run build:kneeboard
          npm run test:kneeboard
      - name: Build and validate package
        shell: pwsh
        run: |
          ./scripts/Build-OvGME.ps1 -Version $env:PACKAGE_VERSION
          ./scripts/Test-Package.ps1 -Version $env:PACKAGE_VERSION
      - uses: actions/upload-artifact@v4
        with:
          name: F4U-1D-OvGME-package-${{ steps.version.outputs.tag }}
          path: |
            dist/*.zip
            dist/SHA256SUMS.txt
      - name: Create tagged release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $Title = "DCS F4U-1D Corsair Components $env:PACKAGE_VERSION"
          $Assets = Get-ChildItem dist/*.zip, dist/SHA256SUMS.txt | ForEach-Object FullName
          gh release create $env:RELEASE_TAG $Assets --target $env:RELEASE_SHA --title $Title --notes-file packaging/release/RELEASE-NOTES.md --repo $env:GITHUB_REPOSITORY
          if ($LASTEXITCODE -ne 0) { throw "Failed to create release $env:RELEASE_TAG." }
